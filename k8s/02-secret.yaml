# ═══════════════════════════════════════════════════════════════
# Secret for SoIce MES Platform
# @author Moon Myung-seop
#
# IMPORTANT: This is a template. DO NOT commit real secrets to Git!
# Use kubectl create secret or external secret management tools.
# ═══════════════════════════════════════════════════════════════

apiVersion: v1
kind: Secret
metadata:
  name: soice-mes-secret
  namespace: soice-mes
  labels:
    app: soice-mes-platform
type: Opaque
stringData:
  # Database Credentials
  POSTGRES_USER: "soice_admin"
  POSTGRES_PASSWORD: "CHANGE_ME_IN_PRODUCTION"

  # Redis Password
  REDIS_PASSWORD: "CHANGE_ME_IN_PRODUCTION"

  # JWT Secret (should be a long random string)
  JWT_SECRET: "CHANGE_ME_IN_PRODUCTION_USE_STRONG_SECRET_KEY"

  # Database Connection URL
  SPRING_DATASOURCE_URL: "jdbc:postgresql://postgres-service:5432/soice_mes"
  SPRING_DATASOURCE_USERNAME: "soice_admin"
  SPRING_DATASOURCE_PASSWORD: "CHANGE_ME_IN_PRODUCTION"

---
# ═══════════════════════════════════════════════════════════════
# How to create secrets securely:
# ═══════════════════════════════════════════════════════════════
#
# Option 1: Using kubectl directly
# kubectl create secret generic soice-mes-secret \
#   --from-literal=POSTGRES_USER=soice_admin \
#   --from-literal=POSTGRES_PASSWORD=your-strong-password \
#   --from-literal=REDIS_PASSWORD=your-redis-password \
#   --from-literal=JWT_SECRET=your-jwt-secret-key \
#   --namespace=soice-mes
#
# Option 2: Using a .env file (not committed to git)
# kubectl create secret generic soice-mes-secret \
#   --from-env-file=.env.production \
#   --namespace=soice-mes
#
# Option 3: Use external secret management (recommended for production)
# - AWS Secrets Manager
# - HashiCorp Vault
# - Azure Key Vault
# - Google Secret Manager
# ═══════════════════════════════════════════════════════════════
